# Python
.venv/
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local SQLite database (rebuilt from seed scripts — never committed)
backend/kavach.db
backend/tests/**/kavach.db
*.db-journal
*.sqlite
*.sqlite3

# Local database backups / dev snapshots (large binaries, not required to run)
data/backups/

# Node / frontend build output
node_modules/
frontend/dist/
frontend/coverage/

# Local env files (use frontend/.env.example as the template)
.env
frontend/.env

# OS / editor cruft
.DS_Store
Thumbs.db
